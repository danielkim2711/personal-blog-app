import PostItem from './PostItem';

const PostList = () => {
  return (
    <>
      <p className='p-2 border-b-4 font-semibold'>RECENT POSTS</p>
      {/* {tasks.map((task) => (
        <PostItem
          key={task.id}
          task={task}
          onDelete={onDelete}
          onToggle={onToggle}
        />
      ))} */}
    </>
  );
};

export default PostList;
